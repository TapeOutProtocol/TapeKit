# HashPort 纯链上网站规范 v0.1（草案）

- 状态：草案，2026-09-13
- 适用链：BNB Smart Chain 主网（chainId 56）
- 许可：本规范文本以 CC0 放弃权利（见 `LICENSE-SPEC`）；参考实现（`kernel/`、`viewer/`、`extension/`、`sw-gateway/`）以 MIT 许可开源（见 `LICENSE`）
- 关键词：**必须**、**不得**、**应当**、**可以**，含义同 RFC 2119 的 MUST / MUST NOT / SHOULD / MAY

## 0. 这份规范解决什么

HashPort 的网站文件本来就完整存在 BNB 链上（SiteRegistry 合约，挂在 TapeOut 电路容器名下）。以前访问要经过「域名 → DNS TXT → 网关」。本规范定义另一条路：**客户端直接读链，不经过 DNS，也不经过我们的网关**。

规范只管四件事：

1. **网址格式**：怎么写一个链上网站的地址（§2）；
2. **地址解析**：从地址找到链上的容器和文件（§3、§4）；
3. **校验**：怎么确认读到的每个字节都与链上一致（§5、§6）；
4. **网站隔离**：客户端怎么运行这些网站才不会互相伤害、不会伤害用户（§7）。

规范**不**管：文件怎么上传（已有 SiteRegistry）、普通浏览器怎么访问（已有网关和域名绑定）、钱包的具体实现。

## 1. 名词

| 名词 | 含义 |
|---|---|
| 处理器 | TapeOut 里的一台 CPU，链上是一个独立的电路 NFT 合约（`Circuits`） |
| 处理器编号 | 处理器在工厂合约 `cpus` 数组里的下标：`factory.cpuAt(编号)`。从 **0** 开始，按创建顺序排列，**只增不改、永不重复**。0 号是创世处理器 Genesis CPU |
| #ID | 一枚电路在它所属处理器里的编号（NFT tokenId），**每个处理器各自从 1 开始编号** |
| 容器 | 这枚电路绑定的 ERC-6551 账户，地址由「链、处理器合约、#ID」唯一算出（`opener.accountOf(处理器, #ID)`）。一个容器一个网站 |
| 网站仓库 | `SiteRegistry` 合约，按（容器，路径）存文件；每个文件带 SHA-256 |
| 付费合约 | `DomainBinding` 合约，记录「某个名字 × 某个容器」付费到什么时候 |
| 内核 | 读链、解析、校验的库（参考实现 `kernel/`）。不渲染、不连钱包 |
| 外壳 | 用内核做出来、真正显示网站的应用：网页查看器、浏览器扩展、桌面或手机 App、钱包里的 dapp 浏览器 |

## 2. 链上名字与网址

### 2.1 为什么名字里必须带处理器编号

每个处理器的 #ID 都从 1 开始，所以「#12」在 0 号处理器、7 号处理器里各有一枚，是完全不同的两枚电路，也对应两个不同的容器。**只写 #ID 无法确定是哪一个**。

能唯一确定一枚电路的是「处理器 + #ID」。处理器的名字不唯一（工厂不查重名），处理器合约地址又太长，所以名字里用**处理器编号**。

### 2.2 链上名字（规范写法）

```
<#ID>.<处理器编号>.tape
```

- 两段都是十进制，**不得**带前导 0（`0` 本身除外），全部小写；
- #ID ≥ 1，处理器编号 ≥ 0；
- 例：`4246.0.tape` = 0 号处理器（Genesis CPU）的 #4246。

这个名字同时也是付费合约里登记的名字（§3.4），逐字相同。

### 2.3 网址

```
tape://<#ID>.<处理器编号>.tape/<路径>
```

例：`tape://4246.0.tape/index.html`。路径规则见 §6。`tape://` 是给外壳用的协议名；**主机名就是完整的链上名字（带 `.tape` 后缀）**，这样一个网站的来源（origin）恰好是 `tape://4246.0.tape`。

> 为什么主机名不能省掉后缀：Chromium 对所有「标准」协议的主机名都做 IPv4 解析，`4246.0` 会被判成非法地址（4246 超过 255），`1.0` 会被改写成 `1.0.0.0`。带上 `.tape` 后最后一段不是数字，就按普通主机名处理。（2026-09-13 在 Electron 43 上实测。）外壳的地址栏**应当**仍接受不带后缀的 `tape://4246.0/` 写法（§2.4），解析后显示为带后缀的规范网址。

### 2.4 输入框应当接受的写法

外壳的地址栏**应当**接受下列写法，并在解析后显示为 §2.2 的规范名字：

| 写法 | 例 | 含义 |
|---|---|---|
| 规范名字 | `4246.0.tape` | |
| 省略后缀 | `4246.0` | 同上 |
| 网址 | `tape://4246.0.tape/docs/` 或 `tape://4246.0/docs/` | 带路径 |
| 简写 | `#4246@0` 或 `4246@0` | #ID @ 处理器编号 |
| 容器地址 | `0x86DD…eA95` | 反查出名字（§3.2） |
| 处理器合约#ID | `0x50A9…9DD9#4246` | 反查出处理器编号（§3.3） |

任何其他写法都**必须**报错，**不得**猜测。

## 3. 解析规则

### 3.1 主网常量

| 项目 | 地址 |
|---|---|
| 处理器工厂 | `0x68224F668083c29e9800Be2a646d42d18cedF7e2` |
| 容器开通器（Opener） | `0x021745DE2f42A7839d96f2d3634d0294487D81F1` |
| ERC-6551 注册表 | `0x000000006551c19487814612e58FE06813775758` |
| 容器实现 | `0xAf4E78a2257C9c5480c2F8310E3b00437260751d` |
| 网站仓库 SiteRegistry（代理） | `0xd006ffdd5Ae313B17729621A00999cD3C71CE5e6` |
| 付费合约 DomainBinding（代理） | `0x861EE183de2BBE4a6ecf9D15812C123b566a3DB7` |

两个代理合约的**实现地址**客户端必须钉住，见 §4.3。

### 3.2 名字 → 容器

以下调用全部在同一个钉住的区块上执行（§4.2）：

1. `factory.cpuCount()`；处理器编号 ≥ 数量 → 状态 `no-such-cpu`；
2. `factory.cpuAt(处理器编号)` 得到处理器合约；
3. `opener.accountOf(处理器合约, #ID)` 得到容器地址；
4. `处理器合约.ownerOf(#ID)` 回滚 → 状态 `no-such-token`；成功则得到当前持有人；
5. `opener.isOpened(处理器合约, #ID)` 为 false → 状态 `not-opened`；
6. 屏蔽名单命中 → 状态 `blocked`（§9）；
7. 开通判定（§3.4）：未开通 → `unpaid`，开通 → `ok`。

### 3.3 容器地址 / 处理器合约#ID → 名字

容器地址输入：

1. `容器.token()` 返回（chainId, 处理器合约, #ID）；调用失败或 chainId ≠ 56 → `not-tapeout`；
2. `factory.isCPU(处理器合约)` 为 false → `not-tapeout`（不是 TapeOut 的处理器）；
3. 用 `cpuAt` 反查处理器编号（工厂没有反查表，客户端按下标分批扫描并缓存）；
4. `opener.accountOf(处理器合约, #ID)` **必须**等于输入的容器地址，否则 `not-tapeout`。这一步防止有人部署一个自称是容器的合约；
5. 之后同 §3.2 第 4–7 步。

「处理器合约#ID」输入：先做第 2、3 步，再按 §3.2 继续。

### 3.4 开通（收费）判定

- 判据，满足任意一条即开通。两条里的容器都是由名字**推导**出来的那一个，而不是任何人自报的：
  1. `DomainBinding.isLive(规范名字, 容器)` 为 true：为这个链上名字付过费且未到期；
  2. `DomainBinding.isContainerLive(容器)` 为 true：这个容器为**任何**名字或域名付过费且未到期。也就是说，一个容器付一次费，它绑定的域名和它的链上名字都能用。这个函数自 2026-09-13 的实现起提供；在更早的实现上调用会回滚，客户端**必须**把回滚当作 false；
- 所以没有抢注问题：别人用自己的容器为 `4246.0.tape` 付费，客户端也不认，因为这个名字推导出的容器不是他的；
- 付费：容器的有效持有人（持有电路 NFT、容器已开通、没有挂单）调用 `DomainBinding.bind(名字或域名, 容器, 月数)`，`msg.value = 月数 × monthlyFee`。当前月费 0.08 BNB / 30 天，一次最多预付到 10 年后，不退款；
- 历史付费：新实现上线之前付的费，只记在「域名 × 容器」上。任何人都可以调用 `DomainBinding.syncContainer(域名, 容器)`，把这条已有记录的到期时间同步成容器级付费。这个函数只读已有记录，填不了任意值；
- 电路 NFT 转手后，付费记录跟着容器走，新持有人继续享有；
- 这与现有的域名绑定用的是同一个合约、同一个价格，**不需要部署新合约**。

**客户端行为：**

- 官方外壳**必须**只显示 `ok` 状态的网站。`unpaid` 显示「这个链上名字还没开通」，并告诉持有人怎么开通；
- 链上数据本身是公开的，任何人都读得到。收费靠的是「官方内核与遵守本规范的外壳只显示已开通的网站」，不是技术上的读取封锁。这一点**应当**在文档里如实写明。

### 3.5 状态码

| 状态 | 含义 | 外壳行为 |
|---|---|---|
| `ok` | 已开通 | 显示 |
| `unpaid` | 名字未付费或已到期 | 不显示内容，提示开通 |
| `not-opened` | 电路还没开通容器 | 不显示 |
| `no-such-cpu` | 没有这个处理器编号 | 报错 |
| `no-such-token` | 这个处理器下没有这枚电路 | 报错 |
| `not-tapeout` | 不是 TapeOut 电路容器 | 报错 |
| `blocked` | 被本客户端屏蔽 | 显示屏蔽说明 |
| `store-changed` | 仓库合约实现与钉住的不符 | 拒绝读取（§4.3） |

## 4. 节点与一致性

### 4.1 多节点核对

- 每个链上读取**必须**至少由 2 个互相独立的节点给出**完全相同**的结果才采用。「独立」指**不同的运营方**：同一家运营的多个节点只算一个；
- 只要有两个节点给出不同结果，**必须**拒绝，**不得**取多数；
- 一个节点报错或超时，**可以**改问下一个节点；
- 客户端**应当**允许用户换成自己信任的节点，包括自建节点。

参考实现默认的公共节点（都通过浏览器跨域预检、支持批量请求，分属 4 个不同运营方）：`bsc-dataseed.bnbchain.org`（BNB Chain）、`bsc-rpc.publicnode.com`（Allnodes）、`bsc-dataseed1.defibit.io`（Defibit）、`bsc-dataseed1.ninicoin.io`（Ninicoin）。

### 4.2 钉住区块

- 一次打开网站的全部读取**必须**钉在同一个区块高度，避免读到一半站长更新了文件、拼出新旧混合的内容；
- 参考做法：取各节点头部高度里第 2 高的那个，再往回退 2 个块。

### 4.3 钉住仓库实现

两个代理合约可以升级。客户端**必须**读 ERC-1967 实现槽（`0x3608…2bbc`，不能调用合约自己的 `implementation()`：升级后回答这个问题的就是新代码），与客户端内置的「审计过的实现」名单比对。不在名单里就返回 `store-changed` 并拒绝读取，直到客户端更新名单。

当前名单：

- SiteRegistry 实现：`0x1d279D138A4D803378a7d4557c056f1beD53c261`
- DomainBinding 实现：`0xaa226181a6588d3f9AC0035e5f3dBaF311039bCE`（2026-09-13 升级，含容器级付费）；旧实现 `0x4E8684EaEA48b524245B2191DeE451eAa1c1cA94` 仍在名单内以备回滚

## 5. 读取与校验

1. `SiteRegistry.fileInfo(容器, 路径)` 返回（大小，类型，sha256，更新时间，块数）；块数为 0 表示文件不存在；
2. 大小超过 8,400,000 字节（350 块 × 24,000 字节）的文件**不得**读取；
3. 不超过 96 KB 的文件用 `read(容器, 路径)` 一次读完；更大的用 `readRange` 每段 96 KB 分段读，所有分段钉在同一区块；
4. 读完**必须**同时满足两条：长度等于声明的大小，且实际字节的 SHA-256 等于链上记录的 `sha256Hash`。任何一条不满足，都视为「上传中或已损坏」，**不得**当作正常文件显示；
5. 链上 `sha256Hash` 为全 0，表示站长没有声明哈希。外壳**可以**显示，但**必须**标成「未校验」；官方外壳**应当**把整站标为未校验；
6. content-type 只放行常规字符（字母、数字、`. + / ; = -` 和空格），不合规一律当成 `application/octet-stream`。

## 6. 路径规则

1. 去掉 `?` 和 `#` 之后的部分，做百分号解码（坏的 `%xx` → 非法），然后 Unicode NFC 归一化（macOS 文件名是 NFD，浏览器发的是 NFC），合并连续的 `/`；
2. 含控制字符、`.` 段或 `..` 段 → 非法；
3. 空路径、`/`、以 `/` 结尾 → 补 `index.html`；去掉开头的 `/`；
4. 落地顺序：
   1. 精确命中；
   2. 最后一段没有扩展名时，试 `<路径>/index.html`；
   3. 仍然没有、且没有扩展名时，退回站长设置的 `fallbackPath`（给单页应用的前端路由用）；
   4. 否则 404。

## 7. 网站隔离要求

这一节是安全底线。一个链上网站可能是恶意的，外壳**必须**保证它伤害不到用户、伤害不到别的网站、伤害不到外壳自己。

### 7.1 每个网站一个独立来源

站点代码**必须**运行在下面两种环境之一：

- **独立的真实来源**：原生 App 或桌面外壳注册 `tape://` 协议，以规范名字 `<#ID>.<处理器编号>.tape` 作为主机名，每个网站一个 origin，例如 `tape://4246.0.tape`。只有这种环境才能保存数据、才能接钱包（§7.4）；
- **不透明来源（预览模式）**：沙盒 iframe，**不带** `allow-same-origin`。网站无法读写任何持久存储，也碰不到外壳页面；
- **Service Worker 网关（域名承载的真实来源）**：一个普通 https 域名，每个网站一个子域名 `<#ID>-<处理器编号>.<网关域名>`（用横线是因为通配证书只覆盖一层子域）。服务器对所有子域名、所有路径只返回同一个引导页；引导页装上 Service Worker 之后，这个来源下的每个请求都由 Service Worker 在用户浏览器里直接读链、核对、校验，服务器不再经手任何内容。这是普通浏览器（Chrome、Edge、Firefox、Safari，不装扩展、不装 App）能得到真实来源的唯一办法：网站可以保存数据、可以使用用户已装的钱包扩展。参考实现 `sw-gateway/`，见 §7.8。

**不得**让两个不同容器的网站共用一个来源；**不得**让网站和外壳页面共用一个来源。

### 7.2 不在有特权的地方运行站点代码

站点代码**不得**运行在扩展页面（`chrome-extension://…` 且能调用扩展 API）、App 的桥接页面、查看器自身页面这类有特权的环境里。浏览器扩展**必须**使用清单里声明的 sandbox 页面来渲染。

### 7.3 存储

在真实来源下，本地存储、IndexedDB、Cookie、缓存都按来源（也就是按容器）隔离。电路转手后，同一个来源的旧数据还在，外壳**应当**给用户提供「清除这个网站的数据」的入口。

### 7.4 钱包

- 只有在独立真实来源下才**可以**注入钱包（例如 `window.ethereum`）；预览模式**不得**注入；
- 每一次连接或签名请求，钱包界面**必须**显示规范名字（如 `4246.0.tape`）、处理器名字和容器地址，**不得**只显示网页自己写的标题；
- 钱包授权**必须**按来源（按容器）记录，一个网站拿到的授权不能被另一个网站使用。

### 7.5 网络请求

- 预览模式的内容安全策略（CSP）**必须**只放行 `blob:`、`data:` 和外壳配置的 RPC 节点，其余请求一律拦截；
- 外壳**应当**把被拦截的请求列给用户看（这也是「纯链上」的运行时证据，见 §8）；
- 真实来源模式下，外壳**应当**默认同样拦截链外请求，由用户逐个网站选择放行。

### 7.6 导航与弹窗

- 网站**不得**导航外壳的顶层页面；
- 网站内部链接在外壳内打开；指向链外的链接**必须**新开窗口，并提示用户已离开链上网站；
- 弹窗、通知、下载**必须**经过用户确认。

### 7.7 显示身份

外壳的地址栏或标题栏**必须**始终显示规范名字，并显示开通状态和校验状态（§5）。网页自己的 `<title>` 只能作为副标题。

### 7.8 Service Worker 网关的额外要求

- **信任边界**：引导页（`index.html`、`sw.js` 及其导入）是这种模式下唯一需要信任的下载。它**必须**开源、不含任何外部资源，任何人都能用同一份文件架自己的网关；官方扩展**可以**对引导页做哈希钉住。一旦 Service Worker 接管，服务器再也不经手内容，链上文件仍按 §5 逐个校验；
- **节点不属于网关**：读链发生在用户的浏览器里，默认节点就是 §4.1 的公共节点，架网关的人可以在配置里加自己的节点，但仍只算一家运营方的一票，法定人数规则不变；用户可以在状态页换成自己的节点，设置只存在用户浏览器里。带密钥的节点地址**不得**写进引导页这种公开文件；
- **链外请求**：Service Worker 能看到页面发起的所有 fetch、XHR、图片、脚本、样式、iframe 请求，**必须**默认拦截并记录，节点地址除外；用户在状态页逐站放行。WebSocket 与 WebRTC 不经过 Service Worker，拦不住，**应当**在文档里写明；
- **身份显示**：网关没有自己的地址栏，规范名字体现在子域名里（`4246-0` 即 `4246.0.tape`）。每个网站**必须**提供状态页 `/.tape/status`（身份、开通、校验、链外引用、拦截记录、节点情况、设置），状态页和各种说明页**不得**含脚本；响应头**应当**带 `x-tape-name`、`x-tape-status`、`x-tape-sha256`、`x-tape-verified`。钱包扩展的授权按来源记录，一个子域名一个授权（§7.4 满足）；
- **保留路径**：`/sw.js` 和 `/.tape/` 前缀归网关，网站里同名文件不会被读到；
- **兄弟子域名**：Cookie 可以被设到父域名上从而互相污染（localStorage、IndexedDB 按精确主机隔离，不受影响）。正式部署**应当**把网关域名提交到 Public Suffix List，这样每个子域名也是独立的「站点」，浏览器会给独立的进程与存储分区；
- **强制刷新**（Shift+刷新）会绕过 Service Worker 一次，引导页要把这种情况解释给用户，普通刷新即可恢复。

## 8. 「纯链上」标记

只有同时满足下面两条，一个网站才能标成「100% 链上」：

1. **静态扫描**：HTML、CSS 里没有指向链外的资源（`http(s)://`、`//host` 开头的 src、href、`url()`、`@import`、meta 跳转等）；
2. **运行时**：加载和运行期间，没有被 §7.5 的策略拦截到链外请求。

两条都满足，才显示「100% 链上」；否则显示「引用了链外资源」，并列出这些地址。

## 9. 屏蔽与举报

- 链上内容谁都删不掉，但外壳是展示方，**必须**支持屏蔽名单（按容器地址或规范名字），命中返回 `blocked`；
- 名单格式：纯文本，一行一个容器地址或规范名字，`#` 开头为注释；
- 官方外壳**应当**提供举报入口，并公开屏蔽的规则；
- 屏蔽只影响遵守本规范的外壳，不影响链上数据，也不影响其他外壳。

## 10. 缓存与更新

- 文件缓存的键**必须**包含（容器，路径，链上 sha256）；链上哈希变了，缓存自然失效；
- 解析结果（名字 → 容器、开通状态）缓存**不应**超过 60 秒；
- 外壳**可以**监听 `SiteRegistry` 的 `FileSet` 和 `FileRemoved` 事件，提前让缓存失效。公共节点大多不提供事件查询（`eth_getLogs`），参考实现改为定期用普通读调用重读已知文件的 `fileInfo`、`pathCount`、`fallbackPath`，任何一项变化即视为更新。
- 文件缓存**应当**按链上 sha256 内容寻址，可以持久化到本地（IndexedDB / 文件目录），读出时不必重新校验。
- 处理器编号表（编号 ↔ 合约地址）**可以**持久缓存：编号只增不改，缓存永不失效，只需补扫新增的编号。
- 外壳**应当**提供中文和英文两种界面文案；内核参考实现的状态与错误文案均为双语。

## 11. 安全考虑

- **节点作假**：§4.1 的多节点核对能挡住单个节点作假；挡不住所有节点一起作假。追求更高保证的外壳**应当**支持用户自建节点，或者用 `eth_getProof` 按区块头验证状态证明；
- **仓库被恶意升级**：§4.3 钉住实现，fail-closed；
- **站长本人作恶**：「已校验」只说明字节与链上一致，不说明网站可信。所以名字必须一直显示（§7.7），钱包请求必须显示名字（§7.4）；
- **挂单冻结**：电路在市场上挂单期间，站长改不了网站，买家看到什么就买到什么；读取不受影响；
- **钓鱼**：名字全是数字，不存在形近字母造成的钓鱼。但 `4246.0` 和 `4264.0` 仍可能被看错，外壳**应当**同时显示处理器名字和持有人；
- **收费可被绕过**：见 §3.4 最后一条，如实告知。

## 12. 与其他规范的关系

- **BEP 草案《可验证的链上前端》**（`../docs/BEP-verifiable-frontend.md`）：那份规范管的是「普通域名下的网页与链上字节一致」，本规范管的是「不经过域名直接读链」。两者共用同一个网站仓库和同一套哈希；
- **ERC-4804 / ERC-6860（`web3://`）**：`web3://` 按合约调用取网页，需要仓库合约实现 ERC-5219 的 `request()` 接口。现有 SiteRegistry 没有这个接口。将来可以部署一个只读适配合约来兼容，不影响本规范。

## 13. 参考实现

| 部件 | 位置 | 说明 |
|---|---|---|
| 内核 | `kernel/` | 零依赖 ES 模块，浏览器与 Node ≥ 18 通用；多节点核对、钉块、钉实现、SHA-256 校验 |
| 网页查看器 | `viewer/` | 预览模式外壳（沙盒 iframe、不透明来源、严格 CSP），显示身份、开通、校验和纯链上状态 |
| 浏览器扩展 | `extension/` | 地址栏关键词 `tape`，用 sandbox 页面渲染；弹出窗口可校验当前 https 网页是否与链上字节一致 |
| Service Worker 网关 | `sw-gateway/` | 普通浏览器不装任何东西就能用的真实来源：子域名 = 链上名字，引导页装 Service Worker 后全部读链在本机完成；状态页 `/.tape/status`；链外请求默认拦截（§7.8） |
| 桌面浏览器 | （计划中） | Electron 注册 `tape://` 协议。已实测主机名必须带 `.tape` 后缀（§2.3）；未开工 |
| HashPort 网关（不在本仓库） | hashport.org 服务 | 普通浏览器经域名访问的路径（已上线） |

## 14. 测试向量

| 输入 | 期望 |
|---|---|
| `4246.0.tape` | 处理器 0（Genesis CPU），#4246，容器 `0x86DDaEF00401E3F10418398D67D7189fc458eA95` |
| `0x86DDaEF00401E3F10418398D67D7189fc458eA95` | 反查得 `4246.0.tape` |
| `4246.0.tape` 的文件清单 | 只有 `index.html`，fallback 为 `index.html` |
| `index.html` | 756 字节，SHA-256 `0xec444c899bd9229f9173082fff362da66dd297179482a58b30b6f53ce9f7a0b6` |
| `tape://4246.0.tape/some/route` | 落地到 `index.html`（§6 第 4.3 步） |
| `tape://4246.0.tape/missing.png` | 404（有扩展名，不走 fallback） |
| `1.999999` | `no-such-cpu` |
| `999999999.0` | `no-such-token` |
| `0x000000000000000000000000000000000000dEaD` | `not-tapeout` |
| `04246.0`、`0.0.tape`、`4246` | 输入错误 |

`4246.0.tape` 当前是 `ok`（容器已付费，`paidVia: container`，2026-09-13 起）。向量对应链上 2026-09-13 09:44 UTC 更新后的文件；站长再更新时要同步改这里和 `kernel/test/mainnet.test.mjs`。

## 附录 A：外壳合规清单

- [ ] 地址栏接受 §2.4 的写法，显示规范名字
- [ ] 至少 2 个节点结果一致，不一致即拒绝
- [ ] 一次打开钉住同一区块
- [ ] 钉住仓库实现，不符即拒绝
- [ ] 每个文件校验长度与 SHA-256
- [ ] 只显示 `ok` 状态的网站
- [ ] 每个网站独立来源，或不透明来源预览
- [ ] 站点代码不在特权环境里运行
- [ ] 预览模式不注入钱包；真实来源模式下，钱包请求显示规范名字
- [ ] 拦截并展示链外请求；「100% 链上」需要静态和运行时都干净
- [ ] 支持屏蔽名单与举报
- [ ] （Service Worker 网关）引导页开源、无外部资源；状态页无脚本；保留 `/sw.js` 与 `/.tape/`；域名进 Public Suffix List
